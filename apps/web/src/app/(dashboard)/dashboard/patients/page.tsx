import { PatientsPageContent } from '@/components/patients/patients-page';

export const metadata = { title: 'Patients' };

export default function PatientsPage() {
  return <PatientsPageContent />;
}
