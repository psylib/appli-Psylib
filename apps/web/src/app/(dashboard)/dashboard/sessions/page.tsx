import { SessionsPageContent } from '@/components/sessions/sessions-page';

export const metadata = { title: 'Séances' };

export default function SessionsPage() {
  return <SessionsPageContent />;
}
