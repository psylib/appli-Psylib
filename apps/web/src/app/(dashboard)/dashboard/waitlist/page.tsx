import { WaitlistContent } from '@/components/waitlist/waitlist-content';

export const metadata = { title: 'Liste d\'attente' };

export default function WaitlistPage() {
  return <WaitlistContent />;
}
