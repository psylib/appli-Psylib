{
  "buildCommand": "pnpm --filter @psyscale/shared-types build && pnpm --filter web build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
