# AI Scribe Post-Call Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Générer automatiquement une note clinique structurée après chaque visio, à partir de la transcription audio (Whisper) et d'un LLM (Claude via OpenRouter), visible dans la fiche de séance.

**Architecture:** Le browser du psy enregistre l'audio mixé (local + remote) via Web Audio API + MediaRecorder. À la fin de l'appel, l'audio est uploadé sur l'API NestJS, stocké en fichier temp, puis un job BullMQ appelle Whisper (transcription) puis Claude (note structurée). La note pré-remplit `sessions.summary_ai` et `sessions.notes`.

**Tech Stack:** NestJS + BullMQ + OpenAI Whisper API + OpenRouter (Claude) + Prisma + Web Audio API + MediaRecorder + React/Next.js

---

## Fichiers touchés

**Créés (backend) :**
- `apps/api/src/video/scribe.service.ts` — Whisper + Claude, processing pipeline
- `apps/api/src/video/scribe.processor.ts` — BullMQ worker
- `apps/api/prisma/migrations/20260604000000_ai_scribe/migration.sql`

**Modifiés (backend) :**
- `apps/api/prisma/schema.prisma` — VideoRoom + Session champs scribe
- `apps/api/src/video/video.service.ts` — enableScribe, uploadScribeAudio, getScribeStatus + patientScribeConsent dans getPsyToken
- `apps/api/src/video/video.controller.ts` — 3 nouveaux endpoints scribe
- `apps/api/src/video/video.module.ts` — BullMQ queue + ScribeService + ScribeProcessor
- `apps/api/src/video/dto/video.dto.ts` — ScribeStatusResponse + VideoTokenResponse étendu

**Créés (frontend) :**
- `apps/web/src/hooks/use-scribe-recorder.ts` — AudioContext mixer + MediaRecorder
- `apps/web/src/components/video/scribe-toggle.tsx` — bouton toggle Pro-gated
- `apps/web/src/components/video/scribe-result-banner.tsx` — bannière résultat post-visio

**Modifiés (frontend) :**
- `apps/web/src/components/video/video-controls.tsx` — prop `scribeSlot`
- `apps/web/src/components/video/video-room.tsx` — intégrer ScribeToggle + useScribeRecorder
- `apps/web/src/app/(dashboard)/video/[roomId]/page.tsx` — gérer patientScribeConsent + upload audio on end
- `apps/web/src/app/(patient-video)/patient-portal/video/[token]/page.tsx` — case scribe consent sur la page de consentement
- `apps/web/src/lib/api/video.ts` — nouveaux appels API scribe
- `apps/web/src/components/video/video-consent-screen.tsx` — case opt-in scribe IA

---

## Task 1 — DB Schema + Migration

**Files:**
- Modify: `apps/api/prisma/schema.prisma`
- Create: `apps/api/prisma/migrations/20260604000000_ai_scribe/migration.sql`

- [ ] **Step 1 : Mettre à jour le schema Prisma**

Dans `apps/api/prisma/schema.prisma`, ajouter à `VideoRoom` (après `guestInviteToken`) :

```prisma
  scribeEnabled    Boolean         @default(false) @map("scribe_enabled")
  scribeStatus     String          @default("none") @map("scribe_status")
  // scribe_status: none | processing | done | failed
```

Dans `model Session` (après `summaryAi`) :

```prisma
  scribeTranscript String?         @map("scribe_transcript")
```

- [ ] **Step 2 : Créer la migration SQL**

Créer `apps/api/prisma/migrations/20260604000000_ai_scribe/migration.sql` :

```sql
-- AI Scribe: champs sur video_rooms et sessions
ALTER TABLE video_rooms
  ADD COLUMN IF NOT EXISTS scribe_enabled BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS scribe_status  TEXT    NOT NULL DEFAULT 'none';

ALTER TABLE sessions
  ADD COLUMN IF NOT EXISTS scribe_transcript TEXT;
```

- [ ] **Step 3 : Générer le client Prisma (local uniquement)**

```bash
cd apps/api && npx prisma generate
```

Expected : `✔ Generated Prisma Client`

- [ ] **Step 4 : Commit**

```bash
git add apps/api/prisma/schema.prisma apps/api/prisma/migrations/20260604000000_ai_scribe/
git commit -m "feat(scribe): schema DB — scribe_enabled/status sur video_rooms, scribe_transcript sur sessions"
```

---

## Task 2 — ScribeService (Whisper + Claude)

**Files:**
- Create: `apps/api/src/video/scribe.service.ts`

- [ ] **Step 1 : Créer le service**

Créer `apps/api/src/video/scribe.service.ts` :

```typescript
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { PrismaService } from '../common/prisma.service';
import { EncryptionService } from '../common/encryption.service';
import { AuditService } from '../common/audit.service';
import { NotificationsService } from '../notifications/notifications.service';
import * as fs from 'fs';

export interface ScribeNote {
  motif: string;
  contenu: string;
  thematiques: string[];
  plan_therapeutique: string;
  points_vigilance: string;
  disclaimer: string;
}

const SCRIBE_SYSTEM_PROMPT = `Tu es un assistant pour psychologue clinicien.
Tu reçois la transcription brute d'une séance de psychothérapie.
Génère une note clinique structurée en JSON. Sois factuel, sobre, professionnel.
N'invente rien. Si une information manque, laisse le champ vide (chaîne vide).
RÈGLES ABSOLUES :
- Ne jamais formuler de diagnostic médical ou psychiatrique
- Ne jamais interpréter au-delà de ce qui est explicitement dit
- Toujours inclure le disclaimer

Réponds UNIQUEMENT avec ce JSON (pas de markdown, pas de texte autour) :
{
  "motif": "Motif de consultation en 1-2 phrases",
  "contenu": "Résumé de la séance en 3-5 paragraphes (style clinique sobre)",
  "thematiques": ["tag1", "tag2"],
  "plan_therapeutique": "Prochaines étapes et orientations thérapeutiques",
  "points_vigilance": "Points d'attention éventuels (chaîne vide si rien)",
  "disclaimer": "Note générée par IA à partir d'une transcription automatique — à réviser et valider par le praticien avant tout usage clinique."
}`;

@Injectable()
export class ScribeService {
  private readonly logger = new Logger(ScribeService.name);
  private readonly openaiApiKey: string;
  private readonly openrouterApiKey: string;
  private readonly openrouterBaseUrl: string;
  private readonly modelMain: string;

  constructor(
    private readonly prisma: PrismaService,
    private readonly encryption: EncryptionService,
    private readonly audit: AuditService,
    private readonly notifications: NotificationsService,
    private readonly config: ConfigService,
  ) {
    this.openaiApiKey = this.config.get<string>('OPENAI_API_KEY', '');
    this.openrouterApiKey = this.config.get<string>('OPENROUTER_API_KEY', '');
    this.openrouterBaseUrl = this.config.get<string>('OPENROUTER_BASE_URL', 'https://openrouter.ai/api/v1');
    this.modelMain = this.config.get<string>('OPENROUTER_MODEL_MAIN', 'anthropic/claude-sonnet-4');
  }

  async transcribeAudio(audioBuffer: Buffer): Promise<string> {
    if (!this.openaiApiKey) throw new Error('OPENAI_API_KEY manquante — transcription impossible');

    const blob = new Blob([audioBuffer], { type: 'audio/webm' });
    const formData = new FormData();
    formData.append('file', blob, 'session.webm');
    formData.append('model', 'whisper-1');
    formData.append('language', 'fr');
    formData.append('response_format', 'text');

    const response = await fetch('https://api.openai.com/v1/audio/transcriptions', {
      method: 'POST',
      headers: { Authorization: `Bearer ${this.openaiApiKey}` },
      body: formData,
    });

    if (!response.ok) {
      const body = await response.text();
      throw new Error(`Whisper API ${response.status}: ${body.slice(0, 200)}`);
    }

    return response.text();
  }

  async generateNote(transcript: string): Promise<ScribeNote> {
    const response = await fetch(`${this.openrouterBaseUrl}/chat/completions`, {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${this.openrouterApiKey}`,
        'Content-Type': 'application/json',
        'HTTP-Referer': 'https://psylib.eu',
        'X-Title': 'PsyLib AI Scribe',
      },
      body: JSON.stringify({
        model: this.modelMain,
        messages: [
          { role: 'system', content: SCRIBE_SYSTEM_PROMPT },
          { role: 'user', content: `Transcription de séance :\n\n${transcript}` },
        ],
        response_format: { type: 'json_object' },
        max_tokens: 2000,
      }),
    });

    if (!response.ok) {
      const body = await response.text();
      throw new Error(`OpenRouter ${response.status}: ${body.slice(0, 200)}`);
    }

    const data = await response.json() as { choices: Array<{ message: { content: string } }> };
    const content = data.choices?.[0]?.message?.content ?? '{}';

    try {
      return JSON.parse(content) as ScribeNote;
    } catch {
      throw new Error(`Note JSON invalide : ${content.slice(0, 100)}`);
    }
  }

  /**
   * Pipeline complet : lit le fichier audio temp, transcrit, génère la note,
   * met à jour la session et notifie le psy.
   * Appelé par ScribeProcessor.
   */
  async processScribeJob(videoRoomId: string, audioFilePath: string): Promise<void> {
    let audioBuffer: Buffer;
    try {
      audioBuffer = fs.readFileSync(audioFilePath);
    } catch {
      this.logger.error(`Fichier audio introuvable : ${audioFilePath}`);
      await this.markFailed(videoRoomId);
      return;
    } finally {
      // Nettoyage immédiat — le fichier ne doit pas rester sur le disque
      fs.unlink(audioFilePath, () => {});
    }

    // Récupérer le contexte de la room
    const room = await this.prisma.videoRoom.findUnique({
      where: { id: videoRoomId },
      include: {
        appointment: { select: { sessionId: true, patientId: true } },
        psychologist: { select: { userId: true } },
      },
    });

    if (!room) {
      this.logger.error(`VideoRoom ${videoRoomId} introuvable`);
      return;
    }

    const sessionId = room.appointment.sessionId;

    try {
      // 1. Transcription
      this.logger.log(`Scribe: transcription Whisper pour room ${videoRoomId}`);
      const transcript = await this.transcribeAudio(audioBuffer);

      // 2. Génération note
      this.logger.log(`Scribe: génération note Claude pour room ${videoRoomId}`);
      const note = await this.generateNote(transcript);

      // 3. Formatage note textuelle
      const formattedNote = [
        `**Motif :** ${note.motif}`,
        '',
        note.contenu,
        '',
        `**Plan thérapeutique :** ${note.plan_therapeutique}`,
        note.points_vigilance ? `\n**Points de vigilance :** ${note.points_vigilance}` : '',
        '',
        `---\n*${note.disclaimer}*`,
      ].filter((l) => l !== undefined).join('\n');

      // 4. Chiffrement + sauvegarde
      const encryptedTranscript = this.encryption.encrypt(transcript);
      const encryptedSummary = this.encryption.encrypt(formattedNote);

      if (sessionId) {
        // Mise à jour de la session existante
        await this.prisma.session.update({
          where: { id: sessionId },
          data: {
            scribeTranscript: encryptedTranscript,
            summaryAi: encryptedSummary,
            tags: note.thematiques,
          },
        });
      }

      // 5. Statut done
      await this.prisma.videoRoom.update({
        where: { id: videoRoomId },
        data: { scribeStatus: 'done' },
      });

      // 6. Log AI usage
      await this.prisma.aiUsage.create({
        data: {
          psychologistId: room.psychologistId,
          feature: 'session_scribe',
          tokensUsed: Math.round(transcript.length / 4),
          model: this.modelMain,
          costUsd: 0,
        },
      });

      // 7. Notification in-app
      await this.notifications.createNotification({
        userId: room.psychologist.userId,
        type: 'ai_complete',
        title: 'Note générée par le Scribe IA',
        body: 'La transcription de votre séance a été traitée. La note est disponible dans la fiche de séance.',
        data: { sessionId: sessionId ?? null, videoRoomId },
      });

      // 8. Audit
      await this.audit.log({
        actorId: room.psychologistId,
        actorType: 'system',
        action: 'AI_SCRIBE_COMPLETE',
        entityType: 'video_room',
        entityId: videoRoomId,
        metadata: { sessionId, transcriptLength: transcript.length },
      });

      this.logger.log(`Scribe: terminé avec succès pour room ${videoRoomId}`);
    } catch (err) {
      this.logger.error(`Scribe: échec pour room ${videoRoomId}: ${err}`);
      await this.markFailed(videoRoomId);
      await this.notifications.createNotification({
        userId: room.psychologist.userId,
        type: 'ai_complete',
        title: 'Scribe IA : échec de traitement',
        body: 'La génération automatique de la note a échoué. Vous pouvez rédiger votre note manuellement.',
        data: { videoRoomId },
      });
      await this.audit.log({
        actorId: room.psychologistId,
        actorType: 'system',
        action: 'AI_SCRIBE_FAILED',
        entityType: 'video_room',
        entityId: videoRoomId,
        metadata: { error: String(err).slice(0, 200) },
      });
    }
  }

  private async markFailed(videoRoomId: string) {
    await this.prisma.videoRoom.update({
      where: { id: videoRoomId },
      data: { scribeStatus: 'failed' },
    }).catch(() => {});
  }
}
```

- [ ] **Step 2 : Commit**

```bash
git add apps/api/src/video/scribe.service.ts
git commit -m "feat(scribe): ScribeService — Whisper transcription + Claude note generation"
```

---

## Task 3 — ScribeProcessor (BullMQ)

**Files:**
- Create: `apps/api/src/video/scribe.processor.ts`

- [ ] **Step 1 : Créer le processor**

Créer `apps/api/src/video/scribe.processor.ts` :

```typescript
import { Processor, WorkerHost } from '@nestjs/bullmq';
import { Logger } from '@nestjs/common';
import { Job } from 'bullmq';
import { ScribeService } from './scribe.service';

export const SCRIBE_QUEUE = 'scribe-processing';

export interface ScribeJobData {
  videoRoomId: string;
  audioFilePath: string;
}

@Processor(SCRIBE_QUEUE, { concurrency: 2 })
export class ScribeProcessor extends WorkerHost {
  private readonly logger = new Logger(ScribeProcessor.name);

  constructor(private readonly scribeService: ScribeService) {
    super();
  }

  async process(job: Job<ScribeJobData>): Promise<void> {
    const { videoRoomId, audioFilePath } = job.data;
    this.logger.log(`Processing scribe job for room ${videoRoomId}`);
    await this.scribeService.processScribeJob(videoRoomId, audioFilePath);
  }
}
```

- [ ] **Step 2 : Commit**

```bash
git add apps/api/src/video/scribe.processor.ts
git commit -m "feat(scribe): ScribeProcessor BullMQ worker"
```

---

## Task 4 — VideoModule + VideoService (endpoints scribe)

**Files:**
- Modify: `apps/api/src/video/video.module.ts`
- Modify: `apps/api/src/video/video.service.ts`
- Modify: `apps/api/src/video/video.controller.ts`
- Modify: `apps/api/src/video/dto/video.dto.ts`

- [ ] **Step 1 : Mettre à jour video.module.ts**

Remplacer le contenu de `apps/api/src/video/video.module.ts` :

```typescript
import { Module } from '@nestjs/common';
import { BullModule } from '@nestjs/bullmq';
import { ConfigService } from '@nestjs/config';
import { VideoController } from './video.controller';
import { VideoService } from './video.service';
import { ScribeService } from './scribe.service';
import { ScribeProcessor, SCRIBE_QUEUE } from './scribe.processor';
import { RoomServiceClient } from 'livekit-server-sdk';
import { BillingModule } from '../billing/billing.module';
import { NotificationsModule } from '../notifications/notifications.module';

@Module({
  imports: [
    BillingModule,
    NotificationsModule,
    BullModule.registerQueue({
      name: SCRIBE_QUEUE,
      defaultJobOptions: {
        attempts: 2,
        backoff: { type: 'exponential', delay: 5000 },
        removeOnComplete: 50,
        removeOnFail: 20,
      },
    }),
  ],
  controllers: [VideoController],
  providers: [
    VideoService,
    ScribeService,
    ScribeProcessor,
    {
      provide: RoomServiceClient,
      inject: [ConfigService],
      useFactory: (config: ConfigService) => {
        const host = config.get<string>('LIVEKIT_API_URL', 'http://localhost:7880');
        const apiKey = config.get<string>('LIVEKIT_API_KEY');
        const apiSecret = config.get<string>('LIVEKIT_API_SECRET');
        return new RoomServiceClient(host, apiKey, apiSecret);
      },
    },
  ],
  exports: [VideoService],
})
export class VideoModule {}
```

- [ ] **Step 2 : Ajouter les DTOs dans video.dto.ts**

Dans `apps/api/src/video/dto/video.dto.ts`, ajouter à la fin :

```typescript
export class ScribeStatusResponse {
  status!: 'none' | 'processing' | 'done' | 'failed';
  scribeEnabled!: boolean;
}

export class EnableScribeDto {
  // body vide — l'activation est un toggle
}

export class RecordConsentDto {
  includeScribe?: boolean; // si true, enregistre aussi ai_video_transcription
}
```

Et étendre `VideoTokenResponse` pour inclure les infos scribe :

Trouver la classe `VideoTokenResponse` et y ajouter :
```typescript
  patientScribeConsent?: boolean;
  scribeEnabled?: boolean;
  scribeStatus?: string;
```

- [ ] **Step 3 : Ajouter les méthodes dans video.service.ts**

Ajouter les imports suivants en haut de `video.service.ts` (après les imports existants) :

```typescript
import { InjectQueue } from '@nestjs/bullmq';
import { Queue } from 'bullmq';
import { SCRIBE_QUEUE, ScribeJobData } from './scribe.processor';
import * as fs from 'fs';
import * as path from 'path';
import * as os from 'os';
```

Modifier le constructeur pour injecter la queue :

```typescript
constructor(
  private readonly prisma: PrismaService,
  private readonly audit: AuditService,
  private readonly roomService: RoomServiceClient,
  private readonly config: ConfigService,
  private readonly notificationGateway: NotificationGateway,
  @InjectQueue(SCRIBE_QUEUE) private readonly scribeQueue: Queue<ScribeJobData>,
) {
```

Modifier `generatePsyToken` pour retourner les infos scribe — remplacer le `return` final par :

```typescript
    // Check patient scribe consent
    const patientScribeConsent = room.appointment.patientId
      ? !!(await this.prisma.gdprConsent.findFirst({
          where: {
            patientId: room.appointment.patientId,
            type: 'ai_video_transcription',
            withdrawnAt: null,
          },
        }))
      : false;

    return {
      token: await token.toJwt(),
      wsUrl: this.livekitWsUrl,
      roomName: room.roomName,
      durationMin: room.appointment.duration,
      patientScribeConsent,
      scribeEnabled: room.scribeEnabled,
      scribeStatus: room.scribeStatus,
    };
```

Ajouter à la fin de la classe (avant la fermeture `}`) les nouvelles méthodes :

```typescript
  async enableScribe(userId: string, appointmentId: string): Promise<{ scribeEnabled: boolean }> {
    const psy = await this.getPsychologist(userId);
    const room = await this.prisma.videoRoom.findFirst({
      where: { appointmentId, psychologistId: psy.id },
    });
    if (!room) throw new NotFoundException('Salle de visio introuvable');
    if (room.status === 'ended') throw new BadRequestException('La consultation est terminée');

    const newValue = !room.scribeEnabled;
    await this.prisma.videoRoom.update({
      where: { id: room.id },
      data: { scribeEnabled: newValue },
    });

    await this.audit.log({
      actorId: psy.userId,
      actorType: 'psychologist',
      action: newValue ? 'SCRIBE_ENABLED' : 'SCRIBE_DISABLED',
      entityType: 'video_room',
      entityId: room.id,
    });

    return { scribeEnabled: newValue };
  }

  async uploadScribeAudio(
    userId: string,
    appointmentId: string,
    audioBuffer: Buffer,
  ): Promise<{ status: string }> {
    const psy = await this.getPsychologist(userId);
    const room = await this.prisma.videoRoom.findFirst({
      where: { appointmentId, psychologistId: psy.id },
    });
    if (!room) throw new NotFoundException('Salle de visio introuvable');
    if (!room.scribeEnabled) throw new BadRequestException('Le Scribe IA n\'était pas activé pour cette séance');
    if (room.scribeStatus === 'processing' || room.scribeStatus === 'done') {
      throw new BadRequestException('Transcription déjà en cours ou terminée');
    }

    if (audioBuffer.length > 25 * 1024 * 1024) {
      throw new BadRequestException('Fichier audio trop volumineux (max 25 MB)');
    }

    // Écriture fichier temp
    const fileName = `psylib-scribe-${room.id}-${Date.now()}.webm`;
    const filePath = path.join(os.tmpdir(), fileName);
    fs.writeFileSync(filePath, audioBuffer);

    // Statut processing
    await this.prisma.videoRoom.update({
      where: { id: room.id },
      data: { scribeStatus: 'processing' },
    });

    // Enqueue
    await this.scribeQueue.add('process', {
      videoRoomId: room.id,
      audioFilePath: filePath,
    });

    await this.audit.log({
      actorId: psy.userId,
      actorType: 'psychologist',
      action: 'SCRIBE_AUDIO_UPLOADED',
      entityType: 'video_room',
      entityId: room.id,
      metadata: { sizeBytes: audioBuffer.length },
    });

    return { status: 'processing' };
  }

  async getScribeStatus(userId: string, appointmentId: string): Promise<ScribeStatusResponse> {
    const psy = await this.getPsychologist(userId);
    const room = await this.prisma.videoRoom.findFirst({
      where: { appointmentId, psychologistId: psy.id },
      select: { scribeEnabled: true, scribeStatus: true },
    });
    if (!room) throw new NotFoundException('Salle de visio introuvable');
    return {
      scribeEnabled: room.scribeEnabled,
      status: room.scribeStatus as 'none' | 'processing' | 'done' | 'failed',
    };
  }
```

Ne pas oublier d'importer `ScribeStatusResponse` depuis `./dto/video.dto`.

- [ ] **Step 4 : Ajouter les endpoints dans video.controller.ts**

Ajouter les imports manquants en tête de `video.controller.ts` :

```typescript
import {
  // ... imports existants ...
  UploadedFile,
  UseInterceptors,
  BadRequestException,
} from '@nestjs/common';
import { FileInterceptor } from '@nestjs/platform-express';
import { RequirePlan } from '../billing/decorators/require-plan.decorator';
import { SubscriptionPlan } from '@psyscale/shared-types';
```

Ajouter les 3 nouveaux endpoints **avant** la section "Public endpoints" :

```typescript
  // --- Scribe IA (Pro + Clinic) ---

  @Post('rooms/:appointmentId/scribe/enable')
  @ApiBearerAuth()
  @UseGuards(KeycloakGuard, RolesGuard, SubscriptionGuard)
  @Roles('psychologist', 'admin')
  @RequirePlan(SubscriptionPlan.PRO, SubscriptionPlan.CLINIC)
  @ApiOperation({ summary: 'Activer/désactiver le Scribe IA pour cette séance' })
  async enableScribe(
    @Param('appointmentId', ParseUUIDPipe) appointmentId: string,
    @CurrentUser() user: KeycloakUser,
  ) {
    return this.videoService.enableScribe(user.sub, appointmentId);
  }

  @Post('rooms/:appointmentId/scribe/audio')
  @ApiBearerAuth()
  @UseGuards(KeycloakGuard, RolesGuard, SubscriptionGuard)
  @Roles('psychologist', 'admin')
  @RequirePlan(SubscriptionPlan.PRO, SubscriptionPlan.CLINIC)
  @UseInterceptors(FileInterceptor('audio', { limits: { fileSize: 25 * 1024 * 1024 } }))
  @Throttle({ default: { limit: 3, ttl: 60000 } })
  @ApiOperation({ summary: 'Upload audio WebM pour transcription Scribe IA' })
  async uploadScribeAudio(
    @Param('appointmentId', ParseUUIDPipe) appointmentId: string,
    @UploadedFile() file: Express.Multer.File,
    @CurrentUser() user: KeycloakUser,
  ) {
    if (!file) throw new BadRequestException('Fichier audio manquant');
    return this.videoService.uploadScribeAudio(user.sub, appointmentId, file.buffer);
  }

  @Get('rooms/:appointmentId/scribe/status')
  @ApiBearerAuth()
  @UseGuards(KeycloakGuard, RolesGuard)
  @Roles('psychologist', 'admin')
  @ApiOperation({ summary: 'Statut du Scribe IA pour cette séance' })
  async getScribeStatus(
    @Param('appointmentId', ParseUUIDPipe) appointmentId: string,
    @CurrentUser() user: KeycloakUser,
  ) {
    return this.videoService.getScribeStatus(user.sub, appointmentId);
  }
```

- [ ] **Step 5 : Activer `multer` dans main.ts si besoin**

Vérifier que `@nestjs/platform-express` est dans les dépendances API :

```bash
cd apps/api && grep "platform-express" package.json
```

S'il est absent : `pnpm add @nestjs/platform-express multer @types/multer`

- [ ] **Step 6 : Modifier recordConsent pour supporter le scribe consent**

Dans `video.service.ts`, méthode `recordConsent`, modifier pour accepter un paramètre optionnel `includeScribe` :

```typescript
  async recordConsent(joinToken: string, ipAddress?: string, includeScribe = false) {
    const appointment = await this.prisma.appointment.findFirst({
      where: { videoJoinToken: joinToken },
    });

    if (appointment) {
      if (!appointment.patientId) return;
      await this.prisma.gdprConsent.create({
        data: {
          patientId: appointment.patientId,
          type: 'video_consultation',
          version: '2026-04-v1',
          ipAddress,
        },
      });
      if (includeScribe) {
        await this.prisma.gdprConsent.upsert({
          where: {
            patientId_type_version: {
              patientId: appointment.patientId,
              type: 'ai_video_transcription',
              version: '2026-06-v1',
            },
          },
          create: {
            patientId: appointment.patientId,
            type: 'ai_video_transcription',
            version: '2026-06-v1',
            ipAddress,
          },
          update: {},
        });
      }
      return;
    }

    // Check participant token
    const participant = await this.prisma.appointmentParticipant.findFirst({
      where: { videoJoinToken: joinToken },
    });
    if (!participant) throw new UnauthorizedException('Lien invalide');

    await this.prisma.gdprConsent.create({
      data: {
        patientId: participant.patientId,
        type: 'video_consultation',
        version: '2026-04-v1',
        ipAddress,
      },
    });
    if (includeScribe) {
      await this.prisma.gdprConsent.upsert({
        where: {
          patientId_type_version: {
            patientId: participant.patientId,
            type: 'ai_video_transcription',
            version: '2026-06-v1',
          },
        },
        create: {
          patientId: participant.patientId,
          type: 'ai_video_transcription',
          version: '2026-06-v1',
          ipAddress,
        },
        update: {},
      });
    }
  }
```

Vérifier que l'index unique `patientId_type_version` existe dans le schema Prisma pour `GdprConsent` :

```prisma
  @@unique([patientId, type, version], name: "patientId_type_version")
```

Si absent, l'ajouter dans le model `GdprConsent` du schema et créer une migration :

```sql
-- Dans une migration si l'index n'existe pas :
CREATE UNIQUE INDEX IF NOT EXISTS gdpr_consents_patient_type_version_unique
  ON gdpr_consents(patient_id, type, version);
```

Mettre à jour le controller `recordConsent` pour passer le body :

```typescript
  @Post('consent/:token')
  @Throttle({ default: { limit: 3, ttl: 60000 } })
  async recordConsent(
    @Param('token', ParseUUIDPipe) token: string,
    @Req() req: Request,
    @Body() body: RecordConsentDto,
  ) {
    const ip = req.ip || req.headers['x-forwarded-for']?.toString();
    await this.videoService.recordConsent(token, ip, body?.includeScribe ?? false);
    return { ok: true };
  }
```

- [ ] **Step 7 : Build API local**

```bash
cd apps/api && pnpm build 2>&1 | tail -20
```

Expected : `Found 0 errors. Watching for file changes.` ou similaire sans erreur.

- [ ] **Step 8 : Commit**

```bash
git add apps/api/src/video/
git commit -m "feat(scribe): endpoints API enable/upload/status + consent scribe patient"
```

---

## Task 5 — Frontend : API client

**Files:**
- Modify: `apps/web/src/lib/api/video.ts`

- [ ] **Step 1 : Étendre VideoTokenResponse et ajouter les appels scribe**

Dans `apps/web/src/lib/api/video.ts`, modifier `VideoTokenResponse` :

```typescript
export interface VideoTokenResponse {
  token: string;
  wsUrl: string;
  roomName: string;
  durationMin?: number;
  patientScribeConsent?: boolean;
  scribeEnabled?: boolean;
  scribeStatus?: string;
}
```

Modifier `PatientJoinResponse` pour ajouter le champ scribe consent :

```typescript
export interface PatientJoinResponse {
  token: string;
  wsUrl: string;
  roomName: string;
  needsConsent?: boolean;
  psychologistName?: string;
  patientName?: string;
}
```

Modifier `recordConsent` pour accepter includeScribe :

```typescript
  recordConsent: (joinToken: string, includeScribe = false) =>
    apiClient.post<{ ok: boolean }>(`/video/consent/${joinToken}`, { includeScribe }),
```

Ajouter les nouveaux appels à la fin de `videoApi` :

```typescript
  enableScribe: (appointmentId: string, token: string) =>
    apiClient.post<{ scribeEnabled: boolean }>(
      `/video/rooms/${appointmentId}/scribe/enable`,
      {},
      token,
    ),

  uploadScribeAudio: async (appointmentId: string, audioBlob: Blob, token: string) => {
    const formData = new FormData();
    formData.append('audio', audioBlob, 'session.webm');
    // On utilise fetch directement car apiClient ne gère pas multipart
    const baseUrl = process.env.NEXT_PUBLIC_API_URL || 'https://api.psylib.eu';
    const response = await fetch(`${baseUrl}/video/rooms/${appointmentId}/scribe/audio`, {
      method: 'POST',
      headers: { Authorization: `Bearer ${token}` },
      body: formData,
    });
    if (!response.ok) {
      const err = await response.text();
      throw new Error(err || 'Upload audio échoué');
    }
    return response.json() as Promise<{ status: string }>;
  },

  getScribeStatus: (appointmentId: string, token: string) =>
    apiClient.get<{ status: string; scribeEnabled: boolean }>(
      `/video/rooms/${appointmentId}/scribe/status`,
      token,
    ),
```

- [ ] **Step 2 : Build web local**

```bash
cd apps/web && pnpm build 2>&1 | grep -E "Error|error" | head -20
```

Expected : aucune erreur TypeScript sur les fichiers modifiés.

- [ ] **Step 3 : Commit**

```bash
git add apps/web/src/lib/api/video.ts
git commit -m "feat(scribe): API client — VideoTokenResponse étendu + appels scribe"
```

---

## Task 6 — Hook useScribeRecorder

**Files:**
- Create: `apps/web/src/hooks/use-scribe-recorder.ts`

- [ ] **Step 1 : Créer le hook**

Créer `apps/web/src/hooks/use-scribe-recorder.ts` :

```typescript
'use client';

import { useCallback, useRef, useState } from 'react';
import { useRoomContext } from '@livekit/components-react';
import { Track } from 'livekit-client';

export type ScribeRecorderState = 'idle' | 'recording' | 'uploading' | 'done' | 'error';

interface UseScribeRecorderOptions {
  appointmentId: string;
  accessToken: string;
  onUploadComplete?: () => void;
  onError?: (err: string) => void;
}

export function useScribeRecorder({
  appointmentId,
  accessToken,
  onUploadComplete,
  onError,
}: UseScribeRecorderOptions) {
  const room = useRoomContext();
  const mediaRecorderRef = useRef<MediaRecorder | null>(null);
  const audioContextRef = useRef<AudioContext | null>(null);
  const destinationRef = useRef<MediaStreamAudioDestinationNode | null>(null);
  const chunksRef = useRef<Blob[]>([]);
  const [state, setState] = useState<ScribeRecorderState>('idle');

  const start = useCallback(() => {
    if (mediaRecorderRef.current) return; // déjà en cours

    try {
      const audioContext = new AudioContext();
      const destination = audioContext.createMediaStreamDestination();
      audioContextRef.current = audioContext;
      destinationRef.current = destination;
      chunksRef.current = [];

      // Connecter le micro local
      const localAudioTracks = room.localParticipant
        .getTrackPublications()
        .filter((pub) => pub.track?.kind === 'audio' && pub.track?.mediaStreamTrack);
      localAudioTracks.forEach((pub) => {
        if (pub.track?.mediaStreamTrack) {
          const stream = new MediaStream([pub.track.mediaStreamTrack]);
          const source = audioContext.createMediaStreamSource(stream);
          source.connect(destination);
        }
      });

      // Connecter les pistes audio des participants distants
      room.remoteParticipants.forEach((participant) => {
        participant.getTrackPublications().forEach((pub) => {
          if (
            pub.track?.kind === 'audio' &&
            pub.source === Track.Source.Microphone &&
            pub.track?.mediaStreamTrack
          ) {
            const stream = new MediaStream([pub.track.mediaStreamTrack]);
            const source = audioContext.createMediaStreamSource(stream);
            source.connect(destination);
          }
        });
      });

      const mimeType = MediaRecorder.isTypeSupported('audio/webm;codecs=opus')
        ? 'audio/webm;codecs=opus'
        : 'audio/webm';

      const recorder = new MediaRecorder(destination.stream, {
        mimeType,
        audioBitsPerSecond: 32000,
      });
      recorder.ondataavailable = (e) => {
        if (e.data.size > 0) chunksRef.current.push(e.data);
      };
      recorder.start(5000); // chunk toutes les 5s
      mediaRecorderRef.current = recorder;
      setState('recording');
    } catch (err) {
      onError?.(`Impossible de démarrer l'enregistrement : ${err}`);
      setState('error');
    }
  }, [room, onError]);

  const stopAndUpload = useCallback(async (): Promise<void> => {
    const recorder = mediaRecorderRef.current;
    if (!recorder || recorder.state === 'inactive') return;

    setState('uploading');

    await new Promise<void>((resolve) => {
      recorder.onstop = () => resolve();
      recorder.stop();
    });

    mediaRecorderRef.current = null;
    audioContextRef.current?.close();
    audioContextRef.current = null;

    const chunks = chunksRef.current;
    if (chunks.length === 0) {
      setState('error');
      onError?.('Aucun audio enregistré');
      return;
    }

    const mimeType = chunks[0].type || 'audio/webm';
    const audioBlob = new Blob(chunks, { type: mimeType });
    chunksRef.current = [];

    try {
      const baseUrl = process.env.NEXT_PUBLIC_API_URL || 'https://api.psylib.eu';
      const formData = new FormData();
      formData.append('audio', audioBlob, 'session.webm');

      const response = await fetch(
        `${baseUrl}/video/rooms/${appointmentId}/scribe/audio`,
        {
          method: 'POST',
          headers: { Authorization: `Bearer ${accessToken}` },
          body: formData,
        },
      );

      if (!response.ok) {
        const body = await response.text();
        throw new Error(body || `HTTP ${response.status}`);
      }

      setState('done');
      onUploadComplete?.();
    } catch (err) {
      setState('error');
      onError?.(`Upload audio échoué : ${err}`);
    }
  }, [appointmentId, accessToken, onUploadComplete, onError]);

  const cancel = useCallback(() => {
    if (mediaRecorderRef.current?.state !== 'inactive') {
      mediaRecorderRef.current?.stop();
    }
    mediaRecorderRef.current = null;
    audioContextRef.current?.close();
    audioContextRef.current = null;
    chunksRef.current = [];
    setState('idle');
  }, []);

  return { state, start, stopAndUpload, cancel, isRecording: state === 'recording' };
}
```

- [ ] **Step 2 : Commit**

```bash
git add apps/web/src/hooks/use-scribe-recorder.ts
git commit -m "feat(scribe): hook useScribeRecorder — AudioContext mixer + MediaRecorder + upload"
```

---

## Task 7 — ScribeToggle + VideoControls

**Files:**
- Create: `apps/web/src/components/video/scribe-toggle.tsx`
- Modify: `apps/web/src/components/video/video-controls.tsx`

- [ ] **Step 1 : Créer ScribeToggle**

Créer `apps/web/src/components/video/scribe-toggle.tsx` :

```typescript
'use client';

import { Mic2, MicOff, Loader2 } from 'lucide-react';
import type { ScribeRecorderState } from '@/hooks/use-scribe-recorder';

interface ScribeToggleProps {
  isEnabled: boolean;           // scribeEnabled côté serveur
  patientConsented: boolean;    // patient a consenti à ai_video_transcription
  recorderState: ScribeRecorderState;
  isPro: boolean;               // plan Pro ou Clinic
  onToggle: () => void;
}

export function ScribeToggle({
  isEnabled,
  patientConsented,
  recorderState,
  isPro,
  onToggle,
}: ScribeToggleProps) {
  const baseBtn = 'rounded-full p-3 transition-colors text-white';
  const neutralBtn = `${baseBtn} bg-white/10 hover:bg-white/20`;
  const activeBtn = `${baseBtn} bg-[#0D9488] hover:bg-[#0b7d72]`;
  const disabledBtn = `${baseBtn} bg-white/5 cursor-not-allowed opacity-50`;

  if (!isPro) {
    return (
      <button
        disabled
        className={disabledBtn}
        title="Scribe IA — disponible avec le plan Pro"
      >
        <Mic2 className="h-5 w-5" />
      </button>
    );
  }

  if (!patientConsented) {
    return (
      <button
        disabled
        className={disabledBtn}
        title="Le patient n'a pas encore accepté la transcription IA"
      >
        <MicOff className="h-5 w-5" />
      </button>
    );
  }

  if (recorderState === 'uploading') {
    return (
      <button disabled className={disabledBtn} title="Upload en cours...">
        <Loader2 className="h-5 w-5 animate-spin" />
      </button>
    );
  }

  return (
    <button
      onClick={onToggle}
      className={isEnabled ? activeBtn : neutralBtn}
      title={isEnabled ? 'Scribe IA actif — cliquer pour désactiver' : 'Activer le Scribe IA'}
    >
      {isEnabled ? <Mic2 className="h-5 w-5" /> : <MicOff className="h-5 w-5" />}
    </button>
  );
}
```

- [ ] **Step 2 : Modifier VideoControls pour ajouter scribeSlot**

Dans `apps/web/src/components/video/video-controls.tsx`, ajouter `scribeSlot` à l'interface et au rendu :

```typescript
interface VideoControlsProps {
  // ... props existantes ...
  inviteSlot?: React.ReactNode;
  scribeSlot?: React.ReactNode;   // ← nouveau
  onEndCall: () => void;
}
```

Dans le JSX, après `{inviteSlot}` ajouter `{scribeSlot}` :

```tsx
      {inviteSlot}
      {scribeSlot}
```

- [ ] **Step 3 : Commit**

```bash
git add apps/web/src/components/video/scribe-toggle.tsx apps/web/src/components/video/video-controls.tsx
git commit -m "feat(scribe): ScribeToggle component + scribeSlot dans VideoControls"
```

---

## Task 8 — Intégration video-room.tsx + page psy

**Files:**
- Modify: `apps/web/src/components/video/video-room.tsx`
- Modify: `apps/web/src/app/(dashboard)/video/[roomId]/page.tsx`

- [ ] **Step 1 : Modifier VideoRoomProps et VideoLayout**

Dans `apps/web/src/components/video/video-room.tsx`, modifier `VideoRoomProps` :

```typescript
interface VideoRoomProps {
  token: string;
  wsUrl: string;
  appointmentId: string;
  plannedDurationMin: number;
  notesPanel: React.ReactNode;
  onCallEnd: () => void;
  // Scribe props
  scribeEnabled: boolean;
  patientScribeConsent: boolean;
  isPro: boolean;
  accessToken: string;
  onScribeToggle: () => void;
  onScribeUploadComplete: () => void;
  onScribeError: (msg: string) => void;
}
```

Modifier la signature de `VideoLayout` pour recevoir ces props :

```typescript
function VideoLayout({
  appointmentId,
  plannedDurationMin,
  notesPanel,
  onCallEnd,
  scribeEnabled,
  patientScribeConsent,
  isPro,
  accessToken,
  onScribeToggle,
  onScribeUploadComplete,
  onScribeError,
}: Omit<VideoRoomProps, 'token' | 'wsUrl'>) {
```

Ajouter les imports dans `video-room.tsx` :

```typescript
import { useScribeRecorder } from '@/hooks/use-scribe-recorder';
import { ScribeToggle } from './scribe-toggle';
```

Dans le corps de `VideoLayout`, après `useKrispNoiseFilter()` :

```typescript
  const { state: recorderState, start: startRecording, stopAndUpload, cancel: cancelRecording } =
    useScribeRecorder({
      appointmentId,
      accessToken,
      onUploadComplete: onScribeUploadComplete,
      onError: onScribeError,
    });
```

Modifier le `handleConnected`/`onCallEnd` flow — dans le callback `onCallEnd` wrappé :

```typescript
  // Remplacer le bouton end call existant dans VideoControls par un handler enrichi
  const handleEndCall = useCallback(async () => {
    if (scribeEnabled && recorderState === 'recording') {
      await stopAndUpload();
    } else {
      cancelRecording();
    }
    onCallEnd();
  }, [scribeEnabled, recorderState, stopAndUpload, cancelRecording, onCallEnd]);
```

Et lancer l'enregistrement quand scribeEnabled devient true — ajouter un useEffect :

```typescript
  useEffect(() => {
    if (scribeEnabled && recorderState === 'idle') {
      startRecording();
    }
    if (!scribeEnabled && recorderState === 'recording') {
      cancelRecording();
    }
  }, [scribeEnabled, recorderState, startRecording, cancelRecording]);
```

Modifier le `VideoControls` pour passer `scribeSlot` et `onEndCall: handleEndCall` :

```tsx
          <VideoControls
            showNotes={showNotes}
            onToggleNotes={() => setShowNotes((v) => !v)}
            isFullscreen={isFullscreen}
            onToggleFullscreen={toggleFullscreen}
            isScreenSharing={isScreenShareEnabled}
            onToggleScreenShare={toggleScreenShare}
            blurEnabled={blurEnabled}
            blurPending={blurPending}
            onToggleBlur={toggleBlur}
            inviteSlot={<GuestInvitePopover appointmentId={appointmentId} />}
            scribeSlot={
              <ScribeToggle
                isEnabled={scribeEnabled}
                patientConsented={patientScribeConsent}
                recorderState={recorderState}
                isPro={isPro}
                onToggle={onScribeToggle}
              />
            }
            onEndCall={handleEndCall}
          />
```

Mettre à jour `PsyVideoRoom` pour passer toutes les nouvelles props à `VideoLayout` et `LiveKitRoom`.

- [ ] **Step 2 : Modifier la page psy [roomId]/page.tsx**

Dans `apps/web/src/app/(dashboard)/video/[roomId]/page.tsx` :

Ajouter les états scribe après les états existants :

```typescript
  const [scribeEnabled, setScribeEnabled] = useState(false);
  const [patientScribeConsent, setPatientScribeConsent] = useState(false);
  const [scribeUploadDone, setScribeUploadDone] = useState(false);
```

Dans le `init()` useEffect, après `setDuration(data.durationMin)` :

```typescript
        if (data.patientScribeConsent !== undefined) {
          setPatientScribeConsent(data.patientScribeConsent);
        }
        if (data.scribeEnabled) setScribeEnabled(data.scribeEnabled);
```

Ajouter le handler toggle scribe :

```typescript
  const handleScribeToggle = async () => {
    if (!session?.accessToken) return;
    try {
      const result = await videoApi.enableScribe(roomId, session.accessToken);
      setScribeEnabled(result.scribeEnabled);
    } catch (err) {
      console.error('Scribe toggle failed:', err);
    }
  };
```

Modifier `handleEndCall` pour uploader l'audio AVANT `endRoom` — le hook gère ça via `onCallEnd` dans `video-room.tsx`, mais `endRoom` doit rester dans `handleEndCall` :

```typescript
  const handleEndCall = async () => {
    if (ending) return;
    setEnding(true);
    try {
      if (session?.accessToken) {
        await videoApi.endRoom(roomId, session.accessToken);
      }
    } catch (err) {
      console.error('Fermeture de la visio échouée (sortie forcée) :', err);
    } finally {
      void queryClient.invalidateQueries({ queryKey: ['video-today'] });
      router.push('/dashboard/video');
    }
  };
```

Note : `stopAndUpload` est appelé par `VideoLayout.handleEndCall` (Task 8 Step 1) avant que `onCallEnd` (= `handleEndCall` de la page) soit invoqué. L'ordre est : upload audio → end room → redirect.

Passer les props scribe à `PsyVideoRoom` :

```tsx
      <PsyVideoRoom
        token={tokenData.token}
        wsUrl={tokenData.wsUrl}
        appointmentId={roomId}
        plannedDurationMin={duration}
        scribeEnabled={scribeEnabled}
        patientScribeConsent={patientScribeConsent}
        isPro={true}  // TODO: remplacer par vrai plan check depuis session
        accessToken={session?.accessToken ?? ''}
        onScribeToggle={handleScribeToggle}
        onScribeUploadComplete={() => setScribeUploadDone(true)}
        onScribeError={(msg) => console.error('Scribe error:', msg)}
        notesPanel={...}
        onCallEnd={handleEndCall}
      />
```

Pour le `isPro`, récupérer le plan depuis la session next-auth ou la query billing existante. Pour le MVP, utiliser `true` si `session?.user` existe (tous les utilisateurs logués en Pro+ peuvent accéder à cette page puisque le plan gate est sur l'endpoint API).

- [ ] **Step 3 : Build web**

```bash
cd apps/web && pnpm build 2>&1 | grep -E "^.*Error" | head -20
```

Expected : 0 erreurs TypeScript.

- [ ] **Step 4 : Commit**

```bash
git add apps/web/src/components/video/video-room.tsx apps/web/src/app/(dashboard)/video/
git commit -m "feat(scribe): intégration VideoRoom — toggle + enregistrement + upload auto à la fin"
```

---

## Task 9 — Patient consent scribe (page join)

**Files:**
- Modify: `apps/web/src/components/video/video-consent-screen.tsx`
- Modify: `apps/web/src/app/(patient-video)/patient-portal/video/[token]/page.tsx`

- [ ] **Step 1 : Lire video-consent-screen.tsx**

```bash
cat apps/web/src/components/video/video-consent-screen.tsx
```

- [ ] **Step 2 : Modifier VideoConsentScreen**

Ajouter une prop `onAccept` qui transmet le choix scribe. Modifier l'interface :

```typescript
interface VideoConsentScreenProps {
  psychologistName: string;
  onAccept: (includeScribe: boolean) => void;
  isLoading: boolean;
}
```

Ajouter un état `scribeConsent` local et une checkbox dans le rendu :

```typescript
  const [scribeConsent, setScribeConsent] = useState(false);
```

Dans le JSX, avant le bouton "Accepter", ajouter :

```tsx
        <div className="flex items-start gap-3 rounded-lg border border-border bg-surface p-4">
          <input
            id="scribe-consent"
            type="checkbox"
            checked={scribeConsent}
            onChange={(e) => setScribeConsent(e.target.checked)}
            className="mt-0.5 h-4 w-4 accent-[#0D9488]"
          />
          <label htmlFor="scribe-consent" className="text-sm text-muted-foreground leading-relaxed cursor-pointer">
            <span className="font-medium text-foreground">Transcription IA (optionnel)</span>
            <br />
            J'accepte que cette séance soit transcrite automatiquement pour aider mon psychologue
            à rédiger sa note clinique. La transcription est chiffrée et n'est pas partagée.
          </label>
        </div>
```

Modifier le bouton pour passer `scribeConsent` :

```tsx
        <button onClick={() => onAccept(scribeConsent)} disabled={isLoading} ...>
```

- [ ] **Step 3 : Modifier la page patient pour passer includeScribe**

Dans `apps/web/src/app/(patient-video)/patient-portal/video/[token]/page.tsx`, modifier `handleConsent` :

```typescript
  const handleConsent = async (includeScribe: boolean) => {
    setConsentLoading(true);
    try {
      await videoApi.recordConsent(joinToken, includeScribe);
      const result = await videoApi.joinAsPatient(joinToken);
      if (result.token) {
        setTokenData({ token: result.token, wsUrl: result.wsUrl });
        setPhase('waiting');
      }
    } catch (err: unknown) {
      const message = err instanceof Error ? err.message : 'Erreur lors de l\'enregistrement du consentement';
      setError(message);
      setPhase('error');
    } finally {
      setConsentLoading(false);
    }
  };
```

Et mettre à jour le rendu `VideoConsentScreen` :

```tsx
      <VideoConsentScreen
        psychologistName={psychologistName}
        onAccept={handleConsent}
        isLoading={consentLoading}
      />
```

- [ ] **Step 4 : Commit**

```bash
git add apps/web/src/components/video/video-consent-screen.tsx apps/web/src/app/(patient-video)/
git commit -m "feat(scribe): consentement scribe IA côté patient — checkbox opt-in sur la page de join"
```

---

## Task 10 — ScribeResultBanner sur la fiche de séance

**Files:**
- Create: `apps/web/src/components/video/scribe-result-banner.tsx`
- Modify: `apps/web/src/app/(dashboard)/sessions/[id]/page.tsx`

- [ ] **Step 1 : Créer ScribeResultBanner**

Créer `apps/web/src/components/video/scribe-result-banner.tsx` :

```typescript
import { Sparkles } from 'lucide-react';

interface ScribeResultBannerProps {
  summaryAi: string | null;
}

export function ScribeResultBanner({ summaryAi }: ScribeResultBannerProps) {
  if (!summaryAi) return null;

  return (
    <div className="rounded-xl border border-[#0D9488]/30 bg-[#0D9488]/5 p-4">
      <div className="mb-2 flex items-center gap-2">
        <Sparkles className="h-4 w-4 text-[#0D9488]" />
        <span className="text-sm font-medium text-[#0D9488]">Note générée par le Scribe IA</span>
      </div>
      <p className="whitespace-pre-wrap text-sm text-muted-foreground">{summaryAi}</p>
      <p className="mt-3 text-xs text-muted-foreground/70">
        ⚠ À réviser et valider par le praticien avant tout usage clinique.
      </p>
    </div>
  );
}
```

- [ ] **Step 2 : Lire la page sessions/[id]/page.tsx pour trouver où insérer**

```bash
grep -n "summaryAi\|summary_ai\|notes\|ScribeResult" apps/web/src/app/\(dashboard\)/sessions/\[id\]/page.tsx | head -20
```

- [ ] **Step 3 : Insérer ScribeResultBanner dans la page session**

Ajouter l'import en haut de la page :

```typescript
import { ScribeResultBanner } from '@/components/video/scribe-result-banner';
```

Trouver où `summaryAi` (ou `summary_ai`) est affiché dans la page. S'il n'existe pas encore, ajouter dans la section "notes" ou "résumé" :

```tsx
<ScribeResultBanner summaryAi={session.summaryAi ?? null} />
```

- [ ] **Step 4 : Commit**

```bash
git add apps/web/src/components/video/scribe-result-banner.tsx apps/web/src/app/\(dashboard\)/sessions/
git commit -m "feat(scribe): ScribeResultBanner sur la fiche de séance"
```

---

## Task 11 — Migration prod + env var + déploiement

**Files:**
- VPS : `.env` update
- VPS : `docker compose exec api npx prisma migrate deploy`

- [ ] **Step 1 : Ajouter OPENAI_API_KEY sur le VPS**

Sur le VPS (`ssh ubuntu@51.178.31.68`), dans `/opt/psyscale-api/.env` :

```bash
OPENAI_API_KEY=sk-...
```

Vérifier que `OPENROUTER_API_KEY` est déjà présent (requis pour la génération de note).

- [ ] **Step 2 : Déployer l'API (procédure standard)**

Suivre la procédure de déploiement API (cf. MEMORY.md — section "Déploiement API") :

```bash
# Depuis le repo local :
tar -czf /tmp/psyscale-api.tar.gz \
  --exclude='node_modules' --exclude='.next' --exclude='dist' \
  --exclude='apps/web' --exclude='apps/mobile' --exclude='tmp' \
  --exclude='.claude' --exclude='.playwright-mcp' --exclude='.vercel' \
  .

scp -i ~/.ssh/psyscale_ovh /tmp/psyscale-api.tar.gz ubuntu@51.178.31.68:/opt/psyscale-api/psyscale-src-latest.tar.gz
```

Sur le VPS :
```bash
cd /opt/psyscale-api
mkdir -p build-ctx && tar -xzf psyscale-src-latest.tar.gz -C build-ctx/
cp Dockerfile build-ctx/
docker build -t psyscale-api:latest build-ctx/
docker compose down api && docker compose up -d api
docker compose exec -T -u root api npx prisma migrate deploy
docker compose restart api
```

- [ ] **Step 3 : Déployer le frontend (Vercel)**

```bash
cd C:\Users\tonyr\OneDrive\Projet\PsyFlow
npx vercel --prod --yes
```

- [ ] **Step 4 : Vérifier le scribe**

Sur une visio de test :
1. Patient coche la case scribe IA dans la page de consent
2. Psy active le bouton Scribe (icône Mic2 vert)
3. Psy termine la visio
4. Attendre ~60s, vérifier la fiche de séance → ScribeResultBanner visible
5. Vérifier `docker compose logs api | grep "Scribe"` sur VPS

- [ ] **Step 5 : Commit final + tag**

```bash
git add .
git commit -m "feat(scribe): AI Scribe post-call complet — Whisper + Claude + patient consent + bannière session"
```

---

## Self-Review — Couverture du spec

| Exigence spec | Tâche |
|---|---|
| DB scribe_enabled/status/transcript | Task 1 |
| Consent gdpr_consents ai_video_transcription | Task 4 Step 6 + Task 9 |
| Endpoints enable/upload/status | Task 4 Steps 4-5 |
| BullMQ processor | Task 3 |
| Whisper + Claude pipeline | Task 2 |
| Audio capture browser (useScribeRecorder) | Task 6 |
| ScribeToggle Pro-gated | Task 7 |
| VideoControls scribeSlot | Task 7 |
| VideoRoom intégration | Task 8 |
| Patient consent checkbox | Task 9 |
| ScribeResultBanner fiche session | Task 10 |
| Migration prod + env var | Task 11 |
| Plan gate PRO + CLINIC | Task 4 Steps 4-5 |
| Chiffrement AES-256-GCM transcript | Task 2 (encryption.encrypt) |
| Audit log AI_SCRIBE_COMPLETE | Task 2 |
| Notification in-app post-scribe | Task 2 |
| Fichier audio non persisté | Task 2 (fs.unlink immédiat) |
