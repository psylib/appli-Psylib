# n8n — Variables d'environnement production
# Copier ce fichier en .env et remplir les valeurs

# PostgreSQL n8n
N8N_DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# n8n encryption key (générer avec : openssl rand -hex 32)
N8N_ENCRYPTION_KEY=CHANGE_ME_GENERATED_KEY
